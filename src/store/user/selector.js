export const selectUser = (state) => state.user;
export const selectToken = (state) => state.user.token;
export const selectPhotos = (state) => state.user.photos;
export const selectUserId = (state) => state.user.id;
